Raspberry Pi funktionstüchtig machen 

Istallationen
  nginx istalliert
  python istalliert
  virtualeny istalliert
  gunicorn installiert
  pip istalliert (brauch man für django, vereinfacht es Dinge herrunterzuladen)
  django mit Hilfe von pip istalliert

nginx konfigurieren mit
  sudo systemctl start nginx
  sudo systemctl stop nginx
  sudo systemctl restart nginx
  sudo systemctl status nginx
  
  
